# 五分钟医院导诊小车演示 Runbook

> **当前比赛演示基线（2026-07-19）：** 仅启用内科导航点 `internal_medicine`，坐标为 `(8.0, 3.0, 0.0)`；外科只在地图上显示，导航保持禁用。本演示采用简化现场流程，不再要求双人复核或连续三次试跑，但必须保留一名安全员、硬件急停和路线清场。

## 适用范围与安全规则

- 演示主流程：人脸识别 → 欢迎 → 用户问诊 → 推荐内科 → 用户确认 → Nav2 导航 → 避障 → 到达停车 → 语音播报。
- 人脸识别只用于欢迎显示名；相机、识别或低置信度失败时降级为“访客”，不得阻塞导诊。
- 只允许 `internal_medicine` 发起实体导航；`surgery`/外科仅作为地图标注，导航保持禁用。
- `ACTIVE`、HTTP 200、目标已提交和 TTS 播完都不代表到达。
- **到达判定以 Nav2 action 最终状态为准：`SUCCEEDED` 即 `arrived=true`。** Nav2 goal checker 已应用自身位置/角度容差，并在返回成功前停止控制器。`map` 位姿、终点距离和 `0.15 m` 仅保留为诊断信息，不得再次否决 Nav2 成功状态。
- 到达后车辆必须保持停止，并由车端只播报一次：**“已到达内科，请注意脚下。”**
- 硬件急停和现场安全员优先于任何软件状态。软件取消接口已接入 Nav2 action cancel，但截至 2026-07-19 尚未完成实车验证，不能替代硬件急停。

## 演示前检查

1. 将车辆放回固定起点，路线清场，一名安全员随车，硬件急停在手。
2. 确认 PC ASR 服务可达，Jetson 麦克风日志出现 `Mic open, listening...`。
3. 检查：
   ```bash
   curl -fsS http://127.0.0.1:5000/api/nav/stack/status
   curl -fsS http://127.0.0.1:5000/api/nav/pose
   curl -fsS http://127.0.0.1:5000/api/nav/demo/health
   curl -fsS http://127.0.0.1:5000/api/hospital-guide/demo/status
   ```
4. 导航栈要求：`container_running=true`、`stack_ready=true`、`patrol_ready=true`；定位要求 `pose.valid=true`、`pose.frame_id=map`；只读健康接口要求 `success=true`，确认容器内底盘串口可见、IMU 非零且 `/odom` 有实时反馈。
5. 确认内科已启用、外科未启用；不要在本次演示中临时改写正式内科坐标。

### 只读现场预检（不是放行门槛）

```bash
bash scripts/check_hospital_guide_preflight.sh \
  --api-base http://127.0.0.1:5000 \
  --ros-domain 30 \
  --timeout-seconds 10
```

预检只读取 API、传感器反馈和 ROS 图，不启动或停止导航栈，也不发送导航目标；预检通过不等于实车放行，也不能替代现场清场、急停和安全员确认。

## 五分钟演示脚本

1. **00:00–00:30**：启动完整流程，摄像头识别人脸并播报欢迎；识别失败则使用“访客”。
2. **00:30–01:30**：用户描述症状，导诊模块推荐内科。
3. **01:30–02:00**：用户说“带我去内科”或明确确认，系统只发送一次内科导航目标。
4. **02:00–04:30**：安全员随车观察导航与避障。短路线优先测试一个静态障碍；若测试两个障碍，应在路线前半段提前放置并留足间隔。
5. **04:30–05:00**：确认 `/api/nav/demo/goal-status` 返回 `status=SUCCEEDED`、`arrived=true`；车辆停止，车端播报“已到达内科，请注意脚下。”。

## 到达、停车与播报验收

必须同时观察和记录：

- Nav2 action 最终状态为 `SUCCEEDED`；
- `navigation.arrived=true`，会话阶段进入 `ARRIVED`；
- 到达后 `/cmd_vel` 不再持续输出非零速度，车辆保持静止；
- 车载扬声器只播报一次“已到达内科，请注意脚下。”；
- `pose` 与 `distance_m` 作为诊断记录，即使距离略大于 HTTP 层旧容差，也不得让会话卡在 `NAVIGATING`。

如果车辆不动，先查询：

```bash
curl -fsS http://127.0.0.1:5000/api/nav/demo/goal-status
```

- `ACTIVE`：继续检查局部代价地图、路径规划和恢复行为；
- `SUCCEEDED`：表示已经到达并正常停车，不是死机；
- `FAILED` / `CANCELLED`：按导航故障处理，使用硬件停止路径确保安全。

## 恢复

- 相机或人脸失败：降级为访客导诊，不阻塞流程。
- ASR/TTS 失败：安全结束本轮并重新启动完整流程，不伪造识别或播报结果。
- Nav2 或定位失败：报告真实失败原因，确保车辆停止，重新定位后再试。
- 任何急停、障碍物或人工安全判断中断：先使用硬件停止路径，场地重新确认安全后才允许继续。

## 单次最终验收记录

- 演示日期/时间：
- 固定起点：
- 内科目标：`x=8.0, y=3.0, theta=0.0`
- 安全员：
- 急停确认：是 / 否
- Nav2 最终状态：
- 最终 pose / distance：
- 到达后车辆停止：是 / 否
- 到达播报：是 / 否
- 避障结果：
- 备注：
